

#include <Arduino.h>

/*

#include <SPI.h>
#include <SD.h>

extern "C"
{
#include "freertos/FreeRTOS.h"
#include "freertos/timers.h"
}

void someFunction()
{
    // Set up the SD card
    l.debug("Initializing SD card...");
    if (!SD.begin(5))
    {
        l.error("initialization failed!");
        while (1)
            ;
    }
    l.debug("initialization done.");
}

*/